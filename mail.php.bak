<?
	if (! isset($_POST['g-recaptcha-response'])) {
		echo "captcha";
		return;
	}
	
	// configuracion en google: https://www.google.com/recaptcha/admin#site/342086926
	// secret (required)	6LcO1WMUAAAAAObz6ydufkh00D8E5kM2GXkhOR1u
	// usuario: e.stuarts@mas54.com
	
	$secret = '6LcO1WMUAAAAAObz6ydufkh00D8E5kM2GXkhOR1u';
	$captcha = $_POST['g-recaptcha-response'];
	
	$fields = array(
        'secret'    =>  $secret,
        'response'  =>  $captcha,
        'remoteip'  =>  $_SERVER['REMOTE_ADDR']
    );
    $ch = curl_init("https://www.google.com/recaptcha/api/siteverify");
	curl_setopt($ch, CURLOPT_SSL_VERIFYPEER, false);
	curl_setopt($ch, CURLOPT_POST, true);
    curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
    curl_setopt($ch, CURLOPT_TIMEOUT, 15);
    curl_setopt($ch, CURLOPT_POSTFIELDS, http_build_query($fields));
	$response = curl_exec($ch);
    $responseData = json_decode($response);
    curl_close($ch);

	if(!$responseData->success) {
		echo "captcha";
		return;
	}
	$fname 		= $_POST['fname'];
	$email 		= $_POST['email'];
	$subject 	= $_POST['subject'];
	$msg 		= $_POST['msg'];
	$output 	= "De: ".$fname."\n\nEmail: ".$email."\n\nAsunto: ".$subject."\n\nMensaje: ".$msg;
	
	$to 		= 'e.stuarts@mas54.com';
	$headers	= 'Contacto desde la web de Mas54';
	
	
		
	$send = mail($to, $fname, $output."\n\nEste mensaje se recibio desde la seccion de contaco de mas54.com", $headers);
	echo "ok";
	

?>